# HANDOFF — 2026-07-16 · "อัปเดต requirement repo ให้เป็นภาพปัจจุบัน + เคลียร์งานทีละเรื่อง"

> เอกสารส่งต่อสำหรับ **Claude session ใหม่ที่ไม่มี context/memory** ของงานนี้เลย
> อ่านไฟล์นี้จบ = เข้าใจว่าตอนนี้อยู่ตรงไหน ตัดสินใจอะไรไปแล้ว และต้องทำอะไรต่อ
> เจ้าของงาน = คุณฟีน (ลูกค้า) · workspace = `C:\Users\Admin\develyst\smart-scheduler\`
>
> ✅ **DONE รอบนี้ (2026-07-16):** (1) อัปเดตเอกสารตรงโค้ด + (2) **Auto-cut สิ้นวัน (UC-012)** + (3) **Income ceiling เรท/เพดานจริงจาก backoffice (UC-016)** — ทั้งหมด verify จริงบน DB (ดู **§9 Changelog**). ถัดไป: ย้ายแถบสีมาปฏิทิน (UC-016 ส่วนที่เหลือ), payroll/settlement (UC-024), ATTENDED→debit (UC-025), LINE (รอลูกค้าบรีฟ).
>
> 🔴 **INFRA ISSUE ที่เจอ (ต้องแก้ถาวร):** scheduling กับ backoffice-back **ใช้ `drizzle.__drizzle_migrations` (tracking table) ร่วมกัน** ใน DB เดียว → `bun run db:migrate` ของ backoffice ถูกข้าม (คิดว่า applied แล้วเพราะเห็น `when` ของ scheduling) → ops tables ไม่ถูกสร้าง. รอบนี้ผม apply ops schema SQL ตรงๆ (additive, ไม่ลบ) + seed สำเร็จแล้ว **แต่ควรแก้ให้ backoffice ใช้ migrations table แยก** (drizzle-kit `migrations.table`/`migrations.schema` เช่น schema `ops`) กันชนกันถาวร. รายละเอียด §9.

---

## 0. TL;DR — อ่าน 30 วิ

งานที่ลูกค้าสั่งรอบนี้ (เรียงลำดับ):
1. **[กำลังทำ] อัปเดต `requirement.html` + `requirement-timeline.md` ให้ตรงโค้ดจริงปัจจุบัน** — มี badge หลายอันค้าง/ผิด (เช่น UC-029 ยังเขียน Planned ทั้งที่ทำเสร็จแล้ว)
2. **[รอทำต่อ] เคลียร์งานทีละเรื่อง** ตามที่ตกลงดีไซน์ไว้ (§4) — Auto-cut job, Backoffice finance foundation → income ceiling, LINE UX

**ตัดสินใจดีไซน์ที่ล็อคแล้วรอบนี้** (รายละเอียด §4):
- **Auto-cut สิ้นวัน** = ทำ `bun build --compile` exe บางๆ เป็นตัว trigger ยิง endpoint ภายในของ back, logic ตัด+รายงานอยู่ใน `smart-scheduler-back` ที่เดียว, idempotent + run log, timezone = Asia/Bangkok. ตั้ง Windows Task Scheduler รัน ~18:05. **ทำได้เลย ไม่ต้องรอ backoffice**
- **Income ceiling** = ฟีเจอร์ cross-system, **ไม่รอ backoffice-front แต่รอ backoffice-back** (pricing rules + seed เรทจริง + endpoint ดึงเรท/cap + D.1 wire). ให้ทำ **backoffice-back finance foundation ก่อน** เพราะ income ceiling + wallet debit + payroll ใช้ร่วมกัน
- **LINE** = bot inbound ตอบกลับได้จริงแล้ว แต่ยัง **ไทยล้วน (UC-032 Partial)** + **push แจ้งผู้ปกครองตอนสมัครคอร์สยังไม่ทำ (UC-028 Planned)** + UX ดิบ. **ลูกค้าจะบรีฟรายละเอียดก่อนค่อยลงมือ**

---

## 1. บริบทโปรเจค (ย่อ)

**Smart Scheduler** = ระบบแทน Excel + Alis To Soft ให้ **ศูนย์ฝึกกีฬาการทรงตัว/กีฬาล้อ** (Bike, Scooter, Surfskate, Skate ฯลฯ) ของคุณฟีน — **ไม่ใช่โรงเรียนกวดวิชา** (เอกสารเก่าบางที่ยังเขียนผิด). สัญญา **Option C (Ultimate)** มัดจำ 73,000 THB.

### 4 repos ใน workspace เดียว

| Repo | บทบาท | Port | Maturity |
|------|-------|------|----------|
| `smart-scheduler-back` | Scheduling API (source of truth) — `public.*` | 3001 | ~75% |
| `smart-scheduler-front` | Staff calendar UI (Next.js 16 + Mantine v9) | 3000 | ~70% |
| `smart-scheduler-backoffice-back` | Finance/Ops API — `ops.*` | 3002 | ~50-60% BE |
| `smart-scheduler-backoffice-front` | Admin ERP/payroll UI | — | shell เท่านั้น (~Wave 0), feature ~0% |
| `smart-scheduler-requirement` | **control center** (repo นี้) — requirement.html เป็น task board | — | — |

**DB:** PostgreSQL เดียว — `public.*` (scheduling) + `ops.*` (finance). SID/UAT server: https://frontoffice.develyst.online/scheduler/calendar (user `admin` / pass `admin`).

### repo นี้ (requirement) คืออะไร — อ่านก่อนแก้
- `requirement.html` = **traceability hub + task board**. โครง `WF → UC → SCR → API → TC` (+ DIA/DOC) แต่ละอันมี status badge: `Implemented`(เขียว/`status done`) · `Partial`(เหลือง) · `Planned`(เทา). **"งานที่เหลือ" = grep หา `status partial` / `status planned`**
- แก้ badge = แก้ `<span class="status done|partial|planned">` + note ใน `<b>...</b>` ตรงๆ ได้เลย (hand-editable, static HTML)
- Graph section auto-derive จาก card + `.chain` chips — ไม่มี data ต้อง maintain แยก แค่ทำ chain ให้ถูก
- **ห้ามสร้าง `todo.md`/`mastertodo.md`** (ลบไปแล้ว 2026-07-08 แทนด้วย repo นี้)
- ใช้ skill `requirement-hub` (`requirement-hub/SKILL.md`) สำหรับ regenerate ใหญ่ๆ
- **ห้ามแก้ requirement ให้ตรงโค้ดแบบเงียบๆ ถ้าขัดกัน** — hub เป็น plan ไม่ใช่ code mirror; ถ้า code vs spec ขัดกันให้ flag ไว้

---

## 2. ต้นตองานรอบนี้ — ประชุม 2026-07-11

ไฟล์ประชุม: `../meeting-20260711.md` (workspace root). หัวใจคือตาราง "ข้อเสนอแนะ/แนวทางต่อยอด" 5 ข้อ (งานใหม่จริงของรอบนั้น) + เนื้อระบบ workflow 1–5 ที่เล่าไว้

### สถานะ 5 ข้อเสนอแนะ (verify กับโค้ด 2026-07-16)

| # | ที่ประชุมขอ | สถานะจริง |
|---|------------|-----------|
| 1 | UI ภาษาอังกฤษ | ✅ ทำแล้ว — front มี i18n (`src/lib/i18n`) default EN; back seed ชื่อโปรแกรมกีฬาเป็น EN |
| 2 | Multi-branch | ✅ **เปลี่ยนวิธี** — ลูกค้ายืนยัน 07-15 **ไม่เอาแยกสาขา** อยากดูปฏิทินเดียว → สร้าง **Badge system** แทน (migration `0008_badges`, หน้า `/scheduler/badges`). "สาขา" = badge type หนึ่ง |
| 3 | แจ้งลาล่วงหน้าตามประเภทครู | ✅ ทำแล้ว — UC-029 (`lib/leave-notice.ts`): FT/PT ≥1ชม., FL ≥2ชม., admin override, ลาไม่ทัน=409 `LEAVE_NOTICE_TOO_LATE` |
| 4 | Check-in ทางเลือกนอกจาก GPS | ⚠️ **premise ผิด** — โค้ดจริงเป็น **QR token + time-window (เวลากรุงเทพ) ไม่มี GPS** อยู่แล้ว → ต้องถามลูกค้าว่าจริงๆ อยากได้อะไร |
| 5 | Overbook เฉพาะคนเดิม "ลา" | ✅ ทำแล้ว — DB partial-unique index ยกเว้น `SICK_LEAVE`, gate BE+FE |

→ 4/5 เสร็จแล้ว เหลือข้อ 4 (check-in) ที่ต้อง confirm ลูกค้า

---

## 3. สถานะ AS-BUILT ที่ verify แล้ว (สำหรับอัปเดต requirement.html)

requirement.html แก้ล่าสุด 2026-07-15 มี **59 done · 16 partial · 13 planned**. รายการ Partial/Planned ทั้งหมด (ID + สถานะจริงที่ต้องแก้):

### 3A. ฝั่ง Backoffice (verify แล้วโดย sub-agent 2026-07-16 — เชื่อถือได้)

| ID | เดิม | **สถานะจริง** | หลักฐาน / สิ่งที่เหลือ |
|----|------|--------------|----------------------|
| WF-009 Backoffice Wallet/สต๊อก/Payroll | partial | **partial** (BE ~50-60%, FE ~5%) | BE build เยอะแล้ว, FE เป็น shell + placeholder |
| UC-022 / API-018 คลังสินค้า/POS | partial | **BE Implemented / SCR-009 Planned** | `catalog.ts` `GET/POST /api/v1/catalog/items` + `/movements` มี stock logic + idempotency + INSUFFICIENT_STOCK guard (`inventory.service.ts`). Front `inventory/page.tsx` = `<PagePlaceholder>` |
| UC-023 / API-021 / API-022 Wallet+อนุมัติ | partial | **BE Implemented** | `/api/v1/accounts` (+`/ledger`,`/credits`,`/debits`) transactional+idempotent; `/api/v1/commercial/requests` + `PATCH /:id {action:approve\|reject}`. ⚠️ approve `PURCHASE` แค่ flip status ไม่มี debit/provision (มีแต่ TOP_UP ที่ credit จริง). ไม่มี front UI |
| UC-024 / API-023 เรทครู & Payroll | partial | **pricing Partial / payroll Planned** | `/api/v1/pricing/rules` มีแค่ GET+POST+GET/:id (**ไม่มี update/delete**). Settlement/payroll: table `ops.settlement_runs`+`settlement_lines` มีใน schema แต่ **ไม่มี service/route/endpoint เลย** = ยังไม่เริ่ม |
| UC-025 ตัด wallet เมื่อ ATTENDED | planned | **Planned (ไม่ wire ทั้ง 2 ฝั่ง)** | ops มีปลายทาง `POST /accounts/:id/debits` แต่ scheduling ไม่เคยเรียก — ใน `smart-scheduler-back` outbound fetch มีแต่ LINE, ไม่มี ref ไป port 3002. `checkin.service.ts` set ATTENDED แต่ไม่ debit |
| API-019 POST /commerce/sales | partial | **Implemented** | `catalog.ts` `commerceRoutes` `POST /api/v1/commerce/sales` (serviceAuth) `createSale` ตัด stock ต่อ line + idempotency |
| API-020 /api/v1/parties | partial | **Implemented** | `parties.ts` `GET/POST /parties`,`GET/:id`; `ops.parties` มี `external_source`/`external_ref` ลิงก์ teacher/customer ฝั่ง scheduling |
| SCR-009 Backoffice คลังสินค้า/POS | planned | **Planned** | placeholder เท่านั้น |
| SCR-010 Backoffice Wallet/อนุมัติ/Payroll | planned | **Planned** | `wallet/page.tsx`,`payroll/page.tsx` = `<PagePlaceholder>` (Wave 2/3) |

**Seed backoffice:** จริง(ไม่ placeholder)แต่ไม่ครบ — products/pricing จาก rate card จริง (6 catalog items). Teacher: sync party จาก `public.teachers` ครบ 3 type แต่ seed price_rules **เฉพาะ FREELANCE** (HOURLY 500 / Group-Camp 625/1250 / income CAP 20k + ครูโต๊ด weekday 400). **FT/PT ยังไม่มี rate rules**

**backoffice-front:** ไม่ได้ว่างเปล่า 0% — มี scaffold ใช้งานได้ (Next App Router, Mantine `AdminLayout`, `(admin)` route group, `QueryProvider`, Axios client baseURL `http://localhost:3002/api`) **แต่ทุก feature page เป็น `PagePlaceholder`, dashboard KPI hardcode "—", ไม่มี service/hook ต่อ backend เลย** = shell (~Wave 0), feature ~0%

### 3B. ฝั่ง Scheduling (✅ verify แล้วโดย sub-agent 2026-07-16 — final พร้อม path/line)

| ID | เดิม | **สถานะจริง** | หลักฐาน / สิ่งที่เหลือ |
|----|------|--------------|----------------------|
| UC-029 แจ้งลาล่วงหน้า | **planned (ผิด)** | **→ Implemented** | `lib/leave-notice.ts` FT/PT=60min, FL=120min + unit test; wire ใน `scheduler.service.ts:522-535` (throw `LEAVE_NOTICE_TOO_LATE` เว้น `override`) + LINE bot `line-webhook.service.ts:284-290`. **แก้ badge แน่นอน** |
| UC-012 ตัดโควตาสิ้นวัน (auto-cut) | planned | **Planned** (ยังไม่มี cron) | ไม่มี cron/18:00/end-of-day sweep เลย (timer เดียว = LINE outbox poller 15s). logic ตัดต่อคาบมี (`scheduler.service.ts:502-508` `usedSessions++`/voucher `usedHours`) แต่ attendance-driven ไม่ใช่ scheduled → ดู §4.1 |
| UC-016 / TC-015 income cap Freelance | partial | **Partial (mock, ไม่ wire)** | cap math frontend-only ใช้ mock (`front/src/lib/scheduler/teacher.ts:13-40`); real mapper `dtoToTeacher` ไม่ populate `hourlyRate`/`incomeLimit`; back `teachers` table **ไม่มี column `hourly_rate`/`income_limit`** → กับ backend จริง `overLimit` false เสมอ. แถบสีมี (Mantine Progress) แต่อยู่ **หน้า Teachers ไม่ใช่ปฏิทิน**. เหลือ: เรท/cap จริงจาก backoffice + bar บนปฏิทิน → §4.2 |
| SCR-008 / WF-004 LINE OA + QR check-in | partial | **→ Implemented** (token+time-window, ไม่มี GPS) | token ออกตอน confirm (`lib/checkin-token.ts`), validate Bangkok window `[start−30min, end]` (`lib/checkin.ts:15-25`), public `POST /api/checkin` → ATTENDED (`checkin.service.ts:44-67`), wire LINE `qr`/`เช็คอิน` (`line-webhook.service.ts:212-256`), front `/checkin?token=`. **ไม่มี GPS/LBS** |
| UC-028 LINE แจ้งผู้ปกครองสมัครคอร์ส | planned | **Planned (ยังไม่ทำ)** | `createCoursePackage` (`scheduler.service.ts:346-391`) สร้าง package+bookings แต่ **ไม่ enqueue LINE**; `enqueueLine` เรียกเฉพาะ **ครู** ตอน confirm (`:489`). parent มี `line_user_id`+webhook แต่ไม่มี outbound push ตอนสมัครคอร์ส |
| UC-032 LINE Bot TH+EN | partial | **Partial (reply ไทยล้วน)** | `line-webhook.service.ts` มี command set + multi-turn ครบ (register/add-student/นักเรียน/เช็คอิน/ลา/qr/menu); keyword รับ EN alias บ้าง แต่ **reply string เป็นไทยล้วน ไม่มี lang detect**. (i18n TH+EN มีแต่ใน front web ไม่ใช่ bot). ⚠️ ต่างจากที่เข้าใจ: bot **ตอบได้จริง**แต่**ยังไม่ bilingual** — ดู §4.3 |
| UC-027 Bulk Confirm | planned | **Planned** | มีแค่ single `PATCH /api/bookings/:id/status` (`api.ts:50`); ไม่มี bulk endpoint / multi-select UI |
| UC-030 เวลาทำงานครูระดับช่วงเวลา | planned | **Planned (ยัง day-level)** | `teachers.workDays` = `smallint[]` วัน 0–6 เท่านั้น (`schema.ts:126`), enforce day-level `teacherWorksOnDay`. ไม่มี start/end time / time-range table |
| UC-031 แก้/ขยายคอร์สด้วยมือ | planned | **Partial** | move คาบเดี่ยวทำได้ (`moveBooking` `PATCH /api/bookings/:id` `scheduler.service.ts:607-633`) + hand-add คาบ; แต่ไม่มี "extend course/recompute remaining" (`updateCourse` แค่ toggle `adminUnlocked` `:669-682`; append คาบเกิด auto ตอน sick-leave เท่านั้น) |
| UC-033 ชื่อวิชาจริง (Bike/Scooter แยก) | planned | **→ Implemented (มี caveat)** | back seed โปรแกรมกีฬาจริง (`seed-data.ts:8-18`): 1st Trial, Bike/Scooter/Balance Cruiser, Surfskate, Freeskate, Skateboard, Inline Skate, Onewheel, Balance Play. **caveat: Bike กับ Scooter รวมเป็น subject เดียว ไม่ได้แยก** ตามที่ "แยก" สื่อ (front mock ยังมี IELTS placeholder แต่ mock-only) |
| TC-012 daily report/booking create | partial | **Partial (ไม่มี unit test)** | `getDailyReport` (`scheduler.service.ts:221-239`) + `createBooking` ไม่มี `bun test` cover; มีแค่ manual e2e `scripts/smoke.ts`. 13 test files cover leave/voucher/recurring/checkin/badges/webhooks/crm/line แต่ไม่มี report/booking-create |

**⚠️ พบเพิ่ม (ต้อง cleanup):** `smart-scheduler-back/CLAUDE.md` header ยังเขียน "17 endpoints" + อธิบาย flow `PENDING_RESCHEDULE`/B.1 reschedule ที่ **โค้ดลบไปแล้วตาม UC-006** (enum ค้างแต่ flow หายแล้ว) — ควรแก้ให้ตรง

### 3C. Badge system + Multi-branch
- **Badge system: Implemented end-to-end** (verify บน SID) — schema `0008_badges` (`badge_types`→`badge_values`→`booking_badges`, `UNIQUE(booking_id,badge_type_id)`), BE `badge.service.ts` + `/api/badges*` + `PATCH /bookings/:id/badges`, FE หน้า `/scheduler/badges` + calendar chips + filter + dashboard. Design: `../docs/badge-system-design.md`
- **UC-034 Multi-branch: ควร mark เป็น Superseded/Descoped** — ลูกค้าไม่เอาแล้ว แทนด้วย badge (ไม่ใช่ Planned เฉยๆ ต้อง note ว่าเปลี่ยนทิศ)
- **UC-035 Sync Google Calendar / WF-011 แพลตฟอร์มอนาคต:** Planned จริง (อนาคต)

---

## 4. ดีไซน์ที่ตกลงแล้ว 3 เรื่อง (ทำต่อได้เลย)

### 4.1 Auto-cut / end-of-day job (UC-012)
**ไอเดียลูกค้า:** เขียน bun exe แยกทำงานเรื่องเดียว (สิ้นวันสรุปว่ามาเรียนกี่คน/กี่บาท/ตัดกี่คอร์ส) เอาไปลง Windows server ตั้ง Task Scheduler รัน → **ใช้ได้ ถูกทาง**

**ที่ตกลง/ต้องระวัง:**
- **อย่าเขียน logic ซ้ำในตัว exe** → ให้ exe เป็นแค่ **trigger** ยิง `POST /internal/jobs/end-of-day` (ป้องกันด้วย secret header) ใน `smart-scheduler-back`. logic ตัด+รายงานอยู่ใน back ที่เดียว → กดรันมือจาก admin UI / rerun ได้
- **แยก 2 งาน:** (1) **ตัดจริง** — booking ที่คาบผ่านแล้ว + ไม่เช็คอิน + ไม่ลา → no-show + ตัดโควตา; **idempotent** (Task Scheduler ยิงซ้ำได้), transaction, เก็บ `job_runs` log กันตัดซ้ำ (2) **รายงาน** — อ่านอย่างเดียว สร้างจาก state หลังตัด
- **Timezone:** ใช้ Asia/Bangkok (มี `lib/bangkok-time.ts`) ไม่ใช่เวลา local Windows. รัน ~18:05 ประมวลคาบที่ end-time < now และ date = วันนี้
- **Deploy:** `bun build --compile` เป็น exe เดียว, Task Scheduler action=exe, trigger daily 18:05, "Run whether user logged on or not", log stdout ลงไฟล์, retry ทุก 10 นาที x3 ถ้า fail
- **ทำได้เลย ไม่ต้องรอ backoffice**

### 4.2 Income ceiling จริง (UC-016) — cross-system, มี dependency
income ceiling จริงต้องการ 3 อย่าง:
1. เรท/ชม. จริงของ freelance รายคน → `ops.price_rules` (backoffice-back) — **มีบางส่วนแล้ว แต่ FT/PT ยังไม่มี rate**
2. income เดือนนี้ = Σ(ชม.สอนจริง × เรท) → aggregate bookings(scheduling) × rate(ops) = **D.1 wire (ยังไม่มี)**
3. auto-disable เมื่อถึง cap → ฝั่ง scheduling อ่านค่าที่คำนวณมา

**ที่ตกลง:** ไม่รอ backoffice-**front** แต่รอ backoffice-**back**. income ceiling + wallet debit(UC-025) + payroll(UC-024) พึ่ง **foundation เดียวกัน** = pricing/rate ใน backoffice-back + endpoint ดึงเรท + D.1 wire →
**ลำดับแนะนำ: ทำ backoffice-back finance foundation ให้แน่นก่อน (pricing update/delete, seed เรท FT/PT, endpoint ดึงเรท/cap, D.1 debit wire) → แล้ว income ceiling/wallet/payroll ค่อยตามมาง่าย. backoffice-front ทำทีหลัง/ขนานได้**

### 4.3 LINE (UC-032 ฯลฯ)
- LINE bot **inbound ตอบกลับได้จริงแล้ว ทำงานได้** (webhook `lib/line-webhook.ts`, `routes/webhooks.ts`, `services/line-webhook.service.ts`, `lib/line-client.ts`, outbox poller) — ไม่ใช่ "no userId/พัง" ตามเอกสารเก่า
- **แต่ verify แล้วมี 3 gap จริง (ไม่ใช่แค่ UX):**
  1. **reply ไทยล้วน** (UC-032 = Partial) — keyword รับ EN บ้าง แต่ข้อความตอบไม่มี EN / ไม่มี lang switch
  2. **push แจ้งผู้ปกครองตอนสมัครคอร์สยังไม่ได้ทำ** (UC-028 = Planned) — enqueue LINE เฉพาะครูตอน confirm
  3. **UX ดิบ** — command text ล้วน ควรเป็น quick reply / flex / rich menu / LIFF
- **ลูกค้าจะเล่ารายละเอียดเพิ่มก่อน** ค่อยลงมือ → **อย่าเพิ่งทำ LINE UI/push จนกว่าลูกค้าจะบรีฟ**

---

## 5. คำถามค้างต้อง confirm ลูกค้า
1. **Check-in (ข้อ 4 ประชุม):** จริงๆ ต้องการ GPS ไหม หรือ QR+time-window ที่มีพอแล้ว
2. **Voucher:** โค้ด 5/10/15 ชม. vs rate card แพ็ก 1/4/6/10 ชม. ใช้แบบไหน
3. **ครูโต๊ด** อยู่กลุ่ม Part-Time แต่มีเรทแบบ Freelance Private — ยืนยัน
4. เรท Part-Time / base salary FT / ค่าคอมรายคน-ราย SKU ยังไม่ครบ (payroll ต้องใช้)
5. คอร์ส 6 ชม. ขยายสูงสุด week 8 (assumption ในโค้ด) — ยืนยัน

---

## 6. NEXT STEPS (ลำดับแนะนำ)

1. **[งานปัจจุบัน] อัปเดต requirement.html + requirement-timeline.md** ให้ตรง §3 (ยึดผล sub-agent scheduling เมื่อ confirm):
   - UC-029 planned→**done** (แน่นอน)
   - API-018/019/020/021/022 → **done** (BE)
   - API-023 → partial (no update/delete); เพิ่ม note payroll/settlement = planned
   - UC-034 multi-branch → note **Superseded by Badge system**
   - LINE items (UC-032 ฯลฯ) → "Implemented — เหลือ UX polish"
   - อัปเดต count footer/README + timeline entry ใหม่บนสุด
2. **Auto-cut job (UC-012)** ตาม §4.1 — ทำได้เลย เป็นชิ้นปิดจบเร็ว
3. **Backoffice-back finance foundation** ตาม §4.2 — ปลดล็อก income ceiling + wallet + payroll พร้อมกัน
4. **D.1 wire** ATTENDED→debit, ดึง pricing/cap
5. **backoffice-front** ต่อ feature จาก shell ที่มี
6. **LINE UX** — รอลูกค้าบรีฟ

---

## 7. GOTCHAS / ข้อควรรู้
- **ธุรกิจจริง = ศูนย์กีฬาการทรงตัว/ล้อ ไม่ใช่กวดวิชา** — เอกสารเก่าบางที่ยังผิด
- **Mantine Modal render ใน portal** — browser `get_page_text` ที่ scope `<main>` จะไม่เห็น modal (อ่าน card text หรือใช้ API ตรวจแทน)
- dev: `SKIP_AUTH=true` / `SKIP_ADMIN_AUTH=true`. รัน: แต่ละ repo `bun install && bun run dev` (back 3001 / front 3000 / ops-back 3002)
- source of truth เอกสาร: `../docs/` ที่ workspace root (sync copy ไปแต่ละ repo). **ห้ามแก้ `requirement-timeline.md` แยกราย repo**
- อ่านก่อนลงมือ: `../docs/business-domain.md`, `product-catalog-pricing.md`, `teacher-roster-payroll.md`, `requirement-timeline.md` (entry บนสุดชนะ), `<repo>/CLAUDE.md`
- **ห้ามสร้าง todo.md** — backlog = Partial/Planned ใน requirement.html

---

## 8. ไฟล์สำคัญอ้างอิง
- `requirement.html` — task board (repo นี้)
- `requirement-timeline.md` — living spec (repo นี้, DOC-001)
- `../meeting-20260711.md` — ประชุมต้นเรื่อง
- `../docs/badge-system-design.md` — ดีไซน์ badge system
- `../docs/monorepo-overview.md` — architecture (⚠️ ลงวันที่ 2026-06-30 บาง status เก่า เช่น LINE/checkin ที่จริงทำแล้ว)
- `requirement-hub/SKILL.md` — วิธี maintain hub

---

## 9. CHANGELOG — สิ่งที่ทำจริงในเซสชัน 2026-07-16 (ให้ agent อื่นอ่านต่อ)

งานรอบนี้ = **sync เอกสารให้ตรงโค้ด** (ไม่แตะโค้ดฟีเจอร์). ไฟล์ที่แก้:

### `smart-scheduler-requirement/requirement.html` (task board หลัก)
- **flip → Implemented:** UC-029, SCR-008, API-018, API-019, API-020, API-021, API-022 (+ note หลักฐาน path/line)
- **flip → Partial:** UC-031 (move ได้/extend ยังไม่มี), UC-033 (seed จริงแล้ว/Bike-Scooter ยังรวม)
- **refine note (คงสถานะ):** UC-012 (ใส่ดีไซน์ auto-cut exe+endpoint), UC-016 (mock ล้วน + dependency), API-023 (ไม่มี update/delete)
- **UC-034 Multi-branch → `status removed` (Descoped)** — ชี้ไป UC-036
- **เพิ่ม card ใหม่ (Badge system — build เสร็จแต่ hub ไม่เคยมี):** `WF-012`, `UC-036`, `SCR-011`, `API-024`, `TC-016` (chain ครบ, Implemented)
- verify: article balanced 111/111, ไม่มี dangling chain href, status class ครบ (done/partial/planned/removed). counts: **12 WF · 36 UC · 11 SCR · 24 API · 15 TC · 8 DIA** (TC-014 ถูกลบก่อนหน้า ไม่ renumber)

### `smart-scheduler-requirement/README.md`
- อัปเดตบรรทัด counts + สรุปสถานะ (Badge, LINE ไทยล้วน, Backoffice BE done/FE-payroll ยังไม่ทำ, Auto-cut ยังไม่ทำ)

### `smart-scheduler-requirement/CLAUDE.md`
- เพิ่ม pointer บนสุดชี้มา HANDOFF นี้

### `requirement-timeline.md` (living spec) — เพิ่ม entry `2026-07-16` บนสุด **2 ไฟล์**
- `smart-scheduler-requirement/requirement-timeline.md` (DOC-001)
- `docs/requirement-timeline.md` (workspace source of truth)
- ⚠️ **ยังไม่ได้ sync** สำเนาใน `smart-scheduler-back/docs/`, `-front/docs/`, `-backoffice-back/docs/`, `-backoffice-front/docs/` — ถ้าจะให้ครบตาม policy ต้อง copy entry นี้ไปด้วย (หรือรัน merge-workflow.sh)

### `smart-scheduler-back/CLAUDE.md`
- แก้ header stale: "17 endpoints" → ~30; เพิ่ม Done (leave-notice/QR-checkin/CRM/badge); ระบุ reschedule flow + `PENDING_RESCHEDULE` ถูกลบตาม UC-006; ปรับ Remaining

### `smart-scheduler-front/CLAUDE.md`
- แก้ domain stale: "tutoring school" → balance/wheeled-sports center (ส่วน reschedule removal ถูกต้องอยู่แล้ว ไม่แตะ)

### ✅ Auto-cut job (UC-012) — BUILD เสร็จ 2026-07-16 (verify จริงบน DB)
ไฟล์ที่เพิ่ม/แก้ใน `smart-scheduler-back`:
- `src/db/schema.ts` — enum `NO_SHOW` + ตาราง `jobRuns`
- `drizzle/0009_noshow_jobruns.sql` + `drizzle/meta/_journal.json` (idx 9) — **apply แล้วบน DB จริง**
- `src/lib/auto-cut.ts` (pure predicate `isNoShow`/`minutesUntilClassEnd`) + `src/lib/auto-cut.test.ts` (7 tests ผ่าน)
- `src/services/jobs.service.ts` — `runEndOfDayJob(date?)`: CONFIRMED+คาบผ่าน → NO_SHOW + ตัดโควตา (transaction, idempotent, log `job_runs`)
- `src/services/scheduler.service.ts` — เพิ่ม `noShow` ใน `getDailyReport`
- `src/routes/internal.ts` + mount ใน `src/index.ts` — `POST /internal/jobs/end-of-day` (secret `x-internal-secret`, นอก JWT)
- `scripts/end-of-day.ts` — exe trigger (`bun build --compile`) สำหรับ Windows Task Scheduler ~18:05
- `.env.example` — เพิ่ม `INTERNAL_JOB_SECRET`
- **verify:** `bun test` 62/62 ผ่าน · typecheck ผ่าน · smoke: 401 เมื่อ secret ผิด, secret ถูก→run · integration จริง: CONFIRMED คาบผ่าน→NO_SHOW + usedSessions 0→1, rerun ตัดซ้ำ 0
- hub อัปเดต: UC-012→Implemented, WF-004→Implemented, +API-025, +TC-017
- ⚠️ **ต้องทำตอน deploy จริง:** ตั้ง `INTERNAL_JOB_SECRET` ใน env ของ server, `bun build --compile scripts/end-of-day.ts --outfile dist/end-of-day`, ตั้ง Windows Task Scheduler รัน daily ~18:05 · **"กี่บาท" (revenue) ยังไม่รวม** — รอ backoffice pricing

### ✅ Income ceiling — เรท/เพดานจริงจาก backoffice (UC-016) — BUILD เสร็จ 2026-07-16 (verify จริง cross-system)
**ดีไซน์:** ownership ชัด — scheduling เจ้าของชั่วโมง, backoffice เจ้าของเรท/เพดาน; scheduling ดึงเรทมาเติม TeacherDTO → front ใช้ logic เดิม (แถบสี/overLimit) กับเลขจริง. **โฟกัส Freelance** (เรท seed แล้ว) ไม่ต้องรอเรท FT/PT
- **backoffice-back:** `services/pricing.service.ts` `getTeacherRates()` (party→rules + metadata disambiguation, คืนบาท) + route `GET /api/v1/pricing/teacher-rates` (API-026)
- **scheduling-back:** `lib/ops-client.ts` (fetch + cache 5 นาที + gated by `OPS_API_URL` + graceful fail) · `attachTeacherRates()` เรียกใน `getTeachers`+`getCalendar` · `contract.ts` TeacherDTO + `mappers.ts` toTeacherDTO เพิ่ม `hourlyRate`/`incomeLimit` · `.env.example` +`OPS_API_URL`
- **front:** `contract.ts` TeacherDTO + `lib/api/mappers.ts` `dtoToTeacher` map `hourlyRate`/`incomeLimit` (เดิม undefined → mock only)
- **verify (live cross-system):** ทั้ง 3 repo typecheck ผ่าน · backoffice `teacher-rates` คืน freelance 9 คน 500/20000 · scheduling `GET /api/teachers` (OPS_API_URL set) → freelance ได้ rate/cap จริง, non-freelance = null
- hub: UC-016 note (เรท/cap จริงแล้ว, เหลือย้ายแถบสีมาปฏิทิน), +API-026
- ⚠️ **ยังเหลือใน UC-016:** ย้ายแถบสีจากหน้า Teachers มา **ปฏิทิน** · monthlyHours ยังนับฝั่ง front จาก booking ที่โหลด (ควร confirm นิยาม "ชั่วโมงคิด income" กับลูกค้า — ตอนนี้ front นับ booking ในเดือน)

### 🔴 INFRA ISSUE — shared drizzle migrations tracking table (ต้องแก้ถาวร)
- scheduling-back + backoffice-back ชี้ DB เดียวกัน (`154.197.124.206/smart_scheduler`) และ**ใช้ `drizzle.__drizzle_migrations` ร่วมกัน** → migrate ของ backoffice ถูกข้าม (drizzle เทียบ `when` timestamp กับ table ที่มี entry ของ scheduling อยู่แล้ว) → ops tables ไม่ถูกสร้าง
- **workaround รอบนี้:** apply `drizzle/0000_friendly_moonstone.sql` ตรงๆ (ข้าม CREATE SCHEMA, additive ล้วน ไม่ลบ) + `bun run db:seed` สำเร็จ → ops มี 14 ตาราง + 21 parties + freelance rules + 6 catalog
- **แก้ถาวร:** ตั้ง drizzle-kit ของ backoffice ให้ใช้ migrations table แยก (เช่น `migrations: { schema: "ops" }` หรือ table name เฉพาะ) แล้ว re-baseline. **อย่ารัน `bun run db:migrate` ของ backoffice ตรงๆ จนกว่าจะแก้** (จะข้ามอีก)
- หมายเหตุ: backoffice-back เพิ่งได้ `bun install` รอบนี้ (เดิมขาด drizzle-kit + @types/bun)

### 🏗️ Backoffice standalone (item-centric P&L) — ลูกค้าสั่งทำ backoffice 100% ก่อน (2026-07-16)
**การตัดสินใจสถาปัตยกรรม (ลูกค้า):** backoffice = ระบบการเงินแยกเดี่ยว **DB ของตัวเอง** `smart_backoffice_db` คุยผ่าน API เท่านั้น · โมเดล **item-centric**: ทุกอย่างเป็น item มี `item_group`(PRODUCT/SERVICE) + `item_type`(INCOME/EXPENSE/FIXED_COST) · เงินบริษัท = รวม movement (รายได้=INCOME OUT, ต้นทุน=EXPENSE+FIXED_COST OUT) · **ครู freelance = item รายจ่ายมีโควต้า** ตัดทีละชม. (แทน income-ceiling price_rules เดิม) · ตั้ง parties/wallet/settlement เดิมไว้ก่อน (item model แทน)

- **✅ Phase 0 (แยก DB):** สร้าง `smart_backoffice_db`, backoffice `.env` DATABASE_URL ชี้ DB ใหม่, `bun run db:migrate` เข้า DB ใหม่สำเร็จ (แก้ bug shared drizzle tracking ในตัว), แก้ `seed.ts` ไม่อ่าน `public.teachers`. verify: /health + catalog บน DB ใหม่, แยกจาก scheduling สมบูรณ์
- **✅ Phase 1 (item-centric core):** migration `0001_item_pl` (enum item_group/item_type + `stock_movements.amount_minor`), แก้ inventory.service (create ตั้ง group/type, movement บันทึกบาท), เพิ่ม `services/reports.service.ts` + `routes/reports.ts` → **`GET /api/v1/reports/pl?from=&to=`** (revenue/cost/profit/byType/byItem). verify integration จริง: คอร์ส 2×3000=รายได้6000, ครู5ชม.=ต้นทุน2500, ค่าเช่า=50000 → กำไร −46500 ✓ + โควต้าครูตัด 40→35 ✓ · typecheck + 2 tests ผ่าน
- **✅ Phase 2a+2b (frontoffice integration — teacher cost) verified e2e:**
  - **backoffice:** `catalog_items` + `external_ref`/`external_source` (migration `0002`) → item ผูกกับ teacher/entity ได้ · endpoint ใหม่ **`POST /catalog/items/by-ref/movements`** (serviceAuth) ตัด movement ด้วย ref โดยไม่ต้องรู้ uuid · `GET /catalog/items` filter externalSource/externalRef/itemType
  - **scheduling:** `lib/ops-client.ts` เพิ่ม `consumeTeacherHours(teacherId, hours)` (best-effort, ส่ง X-Service-Token) · wire ใน `updateBookingStatus` สาขา attend: ครู FREELANCE ทำงาน 1 คาบ → ตัดโควต้า EXPENSE item 1 หน่วย (post-commit, idempotent `attend:{bookingId}`) · `.env` +`OPS_API_URL`+`SERVICE_TOKEN`
  - **verify e2e จริง cross-DB:** attend freelance → backoffice quota 40→39 + **ต้นทุน 500 ขึ้น P&L** + re-attend ไม่ตัดซ้ำ (39) · typecheck + 62 tests ผ่าน
  - ⚠️ **prod:** scheduling `SERVICE_TOKEN` ต้องตรงกับ backoffice · teacher EXPENSE item + quota ต้องถูกสร้างใน backoffice ก่อน (ตอนนี้สร้างผ่าน API/FE) ไม่งั้น consume เป็น no-op (best-effort)
  - **✅ Phase 2 income side + display retirement — เสร็จครบ + verified:**
    - **income:** `applyStockMovement` รองรับ item ที่ `trackStock=false` (บันทึก movement/รายได้โดยไม่ยุ่ง balance) · ops-client `recordSale(externalRef, qty)` · wire `createCoursePackage`→`recordSale("course-{size}")` + `createVoucher`→`recordSale("voucher-{hours}")` (post-commit, idempotent) · **verify:** ขายคอร์ส→รายได้ 9000, voucher→5000 ขึ้น P&L
    - **display (retire price_rules ceiling → item quota):** ops-client `fetchTeacherRates`→**`fetchTeacherQuotas`** (อ่าน EXPENSE items แทน `/pricing/teacher-rates`) · `attachTeacherRates`→**`attachTeacherQuotas`** set `hourlyRate`+`quotaRemaining`+`overLimit`(quota≤0) · TeacherDTO/mapper: `incomeLimit`→`quotaRemaining`+`overLimit` (ทั้ง back+front) · front `toTeacherView` ใช้ server `overLimit` · `TeachersContent` โชว์ "เหลือโควต้า N ชม." · **verify:** getTeachers → t(quota3)=overLimit false, t(quota0)=overLimit true
    - typecheck ทั้ง 3 repo ผ่าน · scheduling 62 + backoffice 2 tests ผ่าน
    - **🎯 Phase 2 = DONE.** ต่อไป Phase 3 (FE dashboard) + Phase 4 (hardening)
- **✅ Phase 3 (backoffice-front dashboard) — เสร็จ + verify ในเบราว์เซอร์จริง:**
  - **stack:** ต่อยอด shell เดิม (Next 16 + Mantine v9 dark gray-black + Tailwind + TanStack Query + Axios) · เพิ่มฟอนต์ **IBM Plex Mono** (`.font-num` tabular) ให้ตัวเลขการเงินมีบุคลิก ledger/cockpit + `bg-grid` texture + `fadeUp` stagger
  - **layer:** `types/app/backoffice` (item types+labels+thb format) → `services/{catalog,reports}.service.ts` → `hooks/backoffice/{useCatalog,useReports}.ts` → `partials/{Dashboard,Items}` → pages `(admin)/{dashboard,items}` → nav 2 เมนู (งบกำไร-ขาดทุน / รายการ)
  - **P&L Dashboard:** hero กำไรสุทธิ + RingProgress อัตรากำไร + tiles รายรับ/รายจ่าย + แถบแยกตามประเภท + ตาราง by-item · date-range picker
  - **Items:** ตาราง item + filter ตาม type + CreateItemModal (name/sku/group/type/price/trackStock/externalRef) + MovementModal (IN/OUT + preview มูลค่า P&L)
  - **backend tweak:** เพิ่ม `adminOrService` middleware → movements/by-ref/sales รับได้ทั้ง admin (browser) + service (scheduling)
  - **verify (browser จริง):** ขายน้ำ 5 ผ่าน UI → POST movement 201 → stock 24→19 → **dashboard: กำไร ฿75 / รายรับ ฿75 / margin 100%** สด · สร้าง item "ค่าเช่าสถานที่" (FIXED_COST) → POST 201 → ขึ้นในรายการ · ไม่มี console error · typecheck 3 repo ผ่าน
  - หมายเหตุ: preview รันที่ `localhost:3100` (launch.json `backoffice-front`) ต้องมี backoffice-back ที่ 3002 (`SKIP_ADMIN_AUTH=true`) · demo data (water −5, RENT-VENUE) ค้างใน smart_backoffice_db เป็น dev data
- **⏳ Phase 4:** auth จริง (service token + admin JWT), idempotency, reset โควต้ารายเดือน
- ⚠️ **cleanup ค้าง:** schema `ops` เก่าใน `smart_scheduler` (จากตอน verify income-ceiling) ไม่ใช้แล้ว — รอ ok ก่อน drop
- 📝 **doc ค้าง:** requirement.html ยังไม่เพิ่ม card ของโมเดล item-centric ใหม่ (WF/UC/API สำหรับ P&L) — ทำรอบหน้า

### ยังไม่ได้ทำ (งาน build รอบต่อไป — ดู §6)
1. **UC-016 ส่วนที่เหลือ:** ย้ายแถบสี income มาปฏิทิน (front)
2. **Payroll/settlement (UC-024)** + **ATTENDED→wallet debit (UC-025)** — ใช้ `ops-client.ts` ที่มีแล้วต่อยอดได้
3. LINE bilingual + parent-push (UC-028/032) — รอลูกค้าบรีฟ §4.3
4. **แก้ drizzle shared-tracking infra** (🔴 ด้านบน)
5. sync requirement-timeline entry ไป repo docs อื่นๆ (back/front/backoffice)
6. confirm ลูกค้า §5 (check-in GPS?, แยก Bike/Scooter?, voucher pack, เรท FT/PT, นิยามชั่วโมงคิด income)
